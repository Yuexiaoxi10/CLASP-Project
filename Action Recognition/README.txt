Clone repo from the link as mentioned before, add our files into 'act-detector-scripts' folder after cloning

1. 'runTest' shows an example that how to save as .mat file
2. This software will need to compute optical flow, using mex_OF.m to start
3. 'formData.m' is to provide the visualization, '.mat' file is what you saved from python script
